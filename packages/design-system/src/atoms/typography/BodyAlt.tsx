import React from "react";
import Typography from "@material-ui/core/Typography";

const BodyAlt: React.FC = ({ children }) => {
  return <Typography variant="body2">{children}</Typography>;
};
export default BodyAlt;
